package exercise.oop.principles.polimorfism;

public class Shape {

    //В суперклассе(наследуемый класс) создаем метод(функцию), которая будет переопределена в подклассах(наследующих классах)
    public void draw() {}
}
